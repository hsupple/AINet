# --- AINet: code & schema push; living personal DB stays local ---

# Python / OS junk
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
.env
.DS_Store
Thumbs.db
Desktop.ini
*.tmp
.ainet-*.tmp
.ainet-db-backup*/
.ainet-soi-harness*/
.ainet-soi-harness*.log
.changelog-*.tmp

# Living database — keep schema only; everything else is local.
# Seed/reset with: python scripts/reset_db.py --yes
db/**
!db/Rules.txt
!db/Folderrules.json

# Desktop packaging (PyInstaller) — huge binary trees, never commit
build/
dist/
*.spec

# IDE
.idea/
.vscode/
*.swp

# Local ESP32 WiFi / PC IP
esp32/**/config.local.h
